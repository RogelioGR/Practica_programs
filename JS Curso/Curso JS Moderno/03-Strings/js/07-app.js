const product = 'Monitor 20 pulgadas';

console.log(product.toLocaleUpperCase()); // lo transforma a mayuscula 
console.log(product.toLocaleLowerCase()); // lo transforma a minusculas 

const email = "CORREO@CORREO.COM"
console.log(email.toLocaleLowerCase()); 

const precio = 300;
console.log (precio);
console.log (precio.toString());
